import React from "react";

const Overlay = () => {
  return (
    <div className="fixed inset-0 z-40 invisible bg-opacity-25 opacity-0 pointer-events-none bg-text1"></div>
  );
};

export default Overlay;
