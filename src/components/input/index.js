const { default: TextArea } = require("./TextArea");
const { default: Input } = require("./Input");
const { default: InputPassword } = require("./InputPassword");

export { Input, InputPassword, TextArea };
